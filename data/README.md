# Data

`sample_text.txt` is a tiny original corpus for smoke tests only. The default installed copy is also packaged under `kam/data/sample_text.txt`.

Use a larger local text file for meaningful character-language experiments, or run:

```bash
python scripts/download_tiny_shakespeare.py
```

Generated Mackey-Glass, copy-language, and hidden-regime data require no downloaded files.
