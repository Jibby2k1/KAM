# KAM Phase IV Data-Regime Package

Give `KAM_PHASE4_CODEX_DATA_REGIME_BRIEF.md` to Codex as the authoritative scientific and implementation brief.

`phase4_overview.yaml` is a compact machine-readable summary for manifest generation.

The campaign is intentionally mechanism-focused. It does not assume that stochasticity alone explains the existing task differences; it separates regime geometry, randomness source, observability, memory horizon, scale, and freeze-coordinate consistency.
