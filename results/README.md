# Results directory

Commit small JSON/CSV summaries and selected figures here. Avoid committing large checkpoints. Each reported comparison should include the exact command, random seed, hardware, PyTorch version, precision, parameter count, and measured wall-clock time.
