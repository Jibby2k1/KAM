# Phase 6 optimization report

Implemented optimization fixtures include joint parameter partitioning, Cholesky/solve/CG-labelled/blockwise ridge paths, streaming RLS, NLMS, alternating algebra/geometry callbacks, variable-projection modes, trust-region anchor drift checks, algebra transport, dictionary updates, and independently weighted coverage/repulsion/load-balance/drift/conditioning regularizers.

The superseded Stage 1 profile emitted optimizer labels, but the audit found that all labels used the same joint Adam loop. The runner now has explicit ridge, alternating, variable-projection, dictionary, and joint paths; the task-aware replacement (`38040026` / `38040027`) emitted all eight labels with matching `metrics.optimizer_mode` values. Its short fidelity did not reach alternating geometry phases, so its optimizer/geometry means are provisional. The corrected full campaign (`38042710` / `38042711`) records declared and effective schedule steps; all 150 learnable-geometry KAM alternating rows performed a geometry update, while fixed-geometry rows performed none. Solve-based rows remain algebra-path controls and are not directly comparable to geometry-learning rows; see `reports/phase6/PHASE6_STAGE1_FULL_REPORT.md` and `schedule_audit.json`.
